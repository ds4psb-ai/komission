# ===========================================
# KOMISSION FACTORY - Environment Variables
# ===========================================
# ⚠️ SECURITY WARNING:
#   - NEVER commit .env files to git
#   - Use strong passwords and secrets in production
#   - Rotate secrets regularly
#   - Consider using a secrets manager (GCP Secret Manager, AWS Secrets Manager)
#
# Copy this file to .env and fill in your values

POSTGRES_USER=kmeme_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=kmeme_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Redis Cache
REDIS_HOST=localhost
REDIS_PORT=6379

# Neo4j Graph Database
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_neo4j_password

# JWT Security (IMPORTANT: Generate a strong 256-bit secret!)
# Generate with: openssl rand -hex 32
JWT_SECRET_KEY=your_super_secret_key_here_at_least_256_bits

# Google OAuth 2.0
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# AI APIs (Gemini / Claude)
GOOGLE_API_KEY=your_google_api_key
CLAUDE_API_KEY=your_claude_api_key

# AWS S3 (for video/image storage)
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=ap-northeast-2
S3_BUCKET_NAME=kmeme-factory-assets

# Monitoring (optional)
SENTRY_DSN=

# Rate Limiting
# Outlier Crawling
YOUTUBE_API_KEY=your_youtube_api_key_v3
APIFY_API_TOKEN=your_apify_api_token
TIKTOK_RESEARCH_TOKEN=your_tiktok_research_token
INSTAGRAM_ACCESS_TOKEN=your_ig_access_token
IG_BUSINESS_ACCOUNT_ID=your_ig_business_id

# TikTok comment extraction (Playwright)
# Generate a storage state via headful Playwright once, then reuse it here.
TIKTOK_COOKIE_FILE=/path/to/tiktok_storage_state.json
TIKTOK_PLAYWRIGHT_CHANNEL=chrome
TIKTOK_PLAYWRIGHT_HEADFUL=false
TIKTOK_PLAYWRIGHT_USER_DATA_DIR=
TIKTOK_COMMENTS_PROVIDER=comment_list
